# 🤖 دليل وكيل الذكاء الاصطناعي الشامل - MOODi Health

## 📋 نظرة عامة

تم تطوير وكيل ذكاء اصطناعي متكامل يمكنه:
- 💬 الدردشة والإجابة على الأسئلة الصحية
- 📷 تحليل الصور الطبية
- 📅 حجز جلسات مع الأطباء تلقائياً

---

## 🎯 القدرات الجديدة

### 1️⃣ تحليل الصور الطبية 📷

**الميزات:**
- ✅ رفع الصور من المعرض
- ✅ معاينة الصورة قبل الإرسال
- ✅ تحليل ذكي باستخدام AI
- ✅ تقييم درجة الخطورة
- ✅ توصيات طبية

**كيف يعمل:**
1. المستخدم يضغط على أيقونة الصورة 📷
2. يختار صورة من المعرض
3. يمكن إضافة نص توضيحي (اختياري)
4. يرسل للـ AI
5. يتلقى تحليل شامل

**مثال على التحليل:**
```
المستخدم: يرفع صورة + "طفح جلدي في الذراع"
       ↓
AI: "بناءً على الصورة المرفقة، يبدو أن هذا طفح جلدي
    ناتج عن حساسية. التوصيات:

    ✅ استخدام كريم مرطب
    ✅ تجنب الحكة
    ⚠️ إذا استمرت الأعراض، راجع طبيباً

    درجة الخطورة: متوسطة

    هل تريد حجز جلسة مع طبيب الجلدية؟"
```

### 2️⃣ حجز الجلسات تلقائياً 📅

**الميزات:**
- ✅ فهم طلبات الحجز من النص
- ✅ اختيار طبيب متاح تلقائياً
- ✅ جدولة ذكية
- ✅ تأكيد فوري
- ✅ إشعار المستخدم

**أمثلة على الطلبات:**
```
"أريد حجز جلسة مع طبيب"
"احجز لي موعد غداً"
"أحتاج استشارة طبية عاجلة"
"جلسة فيديو مع دكتور"
```

**عملية الحجز:**
```
1. المستخدم: "أريد حجز جلسة"
   ↓
2. AI: يتحقق من الأطباء المتاحين
   ↓
3. AI: يحجز تلقائياً
   ↓
4. AI: "تم حجز جلسة فيديو مع الدكتور أحمد
       بتاريخ 2025-11-19 الساعة 10:00"
   ↓
5. المستخدم: يمكنه عرض الجلسة من التبويب
```

### 3️⃣ التكامل الذكي 🧠

**السيناريوهات:**

#### سيناريو 1: صورة + طلب حجز
```
المستخدم: 📷 صورة + "أشعر بألم، أريد استشارة"
    ↓
AI: يحلل الصورة
AI: يكتشف الخطورة
AI: يقترح حجز جلسة
المستخدم: يوافق
AI: يحجز تلقائياً ✅
```

#### سيناريو 2: حجز مباشر
```
المستخدم: "احجز لي جلسة غداً الساعة 3"
    ↓
AI: يتحقق من التوفر
AI: يحجز الجلسة
AI: يؤكد الحجز ✅
```

#### سيناريو 3: تحليل فقط
```
المستخدم: 📷 صورة + "ما رأيك؟"
    ↓
AI: يحلل الصورة
AI: يعطي توصيات
AI: يوضح درجة الخطورة
```

---

## 🔧 التطبيق التقني

### الملفات المنشأة

#### 1. `lib/ai-agent.ts`

دوال مساعدة للـ AI Agent:

```typescript
// إرسال رسالة للـ AI
sendMessageToAI(message, userId, history)

// تحليل صورة
analyzeImage(imageBase64, userId, context)

// حجز جلسة
bookSessionViaAI(userId, sessionData)

// الحصول على الأطباء المتاحين
getAvailableDoctors()

// جلسات المستخدم
getUserSessions(userId, limit)
```

#### 2. `app/(tabs)/index.tsx` (محسّن)

الميزات الجديدة:
- ✅ زر رفع الصور 📷
- ✅ معاينة الصورة المختارة
- ✅ عرض الصور في المحادثة
- ✅ بادج للجلسات المحجوزة
- ✅ معالجة الأخطاء المحسّنة

---

## 🌐 n8n Workflows المطلوبة

### Workflow 1: الدردشة الأساسية ✅ (موجود)

**URL:**
```
https://moodi-app.app.n8n.cloud/webhook/8b384f8b-fd82-415c-8566-3e7270f2a1ab/chat
```

**الوظيفة:**
- استقبال رسائل المستخدم
- المعالجة بالـ AI
- إرجاع الرد

**Input:**
```json
{
  "chatInput": "رسالة المستخدم",
  "sessionId": "user-id",
  "history": []
}
```

**Output:**
```json
{
  "output": "رد الذكاء الاصطناعي",
  "action": "none" | "book_session",
  "sessionData": {...}
}
```

### Workflow 2: تحليل الصور 📷 (جديد - مطلوب)

**URL المقترح:**
```
https://moodi-app.app.n8n.cloud/webhook/analyze-image
```

**الوظيفة:**
- استقبال صورة base64
- تحليلها بـ GPT-4 Vision أو Claude
- استخراج النتائج والتوصيات
- تقييم درجة الخطورة

**Input:**
```json
{
  "image": "data:image/jpeg;base64,...",
  "userId": "user-id",
  "context": "وصف إضافي من المستخدم"
}
```

**Output المتوقع:**
```json
{
  "analysis": "التحليل الكامل بالعربية",
  "findings": "الملاحظات الرئيسية",
  "recommendations": "التوصيات",
  "urgency": "low" | "medium" | "high"
}
```

**خطوات الـ Workflow:**

1. **Webhook Node** - استقبال البيانات
2. **Function Node** - استخراج الصورة والسياق
3. **HTTP Request Node** - إرسال للـ AI (OpenAI/Claude)
   ```
   Method: POST
   URL: https://api.openai.com/v1/chat/completions
   Body:
   {
     "model": "gpt-4-vision-preview",
     "messages": [{
       "role": "user",
       "content": [
         {"type": "text", "text": "أنت طبيب استشاري..."},
         {"type": "image_url", "image_url": {"url": "{{image}}"}}
       ]
     }]
   }
   ```
4. **Function Node** - معالجة النتيجة
5. **Return Response** - إرجاع التحليل

---

## 🎨 واجهة المستخدم الجديدة

### التحسينات

#### 1. رسالة الترحيب المحسّنة
```
🏥 مرحباً بك في MOODi Health!

أنا مساعدك الصحي الذكي، يمكنني مساعدتك في:

✅ الإجابة على استفساراتك الصحية
📷 تحليل الصور الطبية
📅 حجز جلسات مع الأطباء

كيف يمكنني مساعدتك اليوم؟
```

#### 2. زر رفع الصور
```
┌─────────────────────────────┐
│ 📷  [نص الرسالة...]    📤  │
└─────────────────────────────┘
```

#### 3. عرض الصور في المحادثة
```
┌───────────────────────┐
│ 👤 المستخدم          │
│ ┌─────────────────┐   │
│ │  [الصورة]       │   │
│ └─────────────────┘   │
│ "ما رأيك في هذا؟"    │
└───────────────────────┘

┌───────────────────────┐
│ 🤖 AI                 │
│ "بناءً على الصورة..."│
│ "التوصيات: ..."      │
└───────────────────────┘
```

#### 4. بادج الجلسة المحجوزة
```
┌───────────────────────┐
│ 🤖 AI                 │
│ "تم حجز جلسة فيديو"  │
│ "الدكتور أحمد"       │
│ ──────────────────    │
│ 📅 جلسة محجوزة  ✅   │
└───────────────────────┘
```

---

## 🔐 الأمان والخصوصية

### حفظ البيانات

**جدول `image_analysis`:**
```sql
CREATE TABLE image_analysis (
  id uuid PRIMARY KEY,
  user_id uuid NOT NULL,
  image_url text NOT NULL,      -- رابط أو معرف
  analysis_result text,          -- نتيجة التحليل
  body_part text,               -- الجزء من الجسم
  created_at timestamptz
);
```

**RLS Policies:**
- المستخدم يرى صوره فقط
- المسؤولون يرون الكل (للإدارة)

---

## 📊 أمثلة على الاستخدام

### مثال 1: استشارة عامة
```
👤: "أعاني من صداع مستمر"
🤖: "الصداع المستمر قد يكون له عدة أسباب:
     • قلة النوم
     • التوتر
     • الجفاف

     التوصيات:
     • شرب الماء بكثرة
     • الراحة
     • تجنب الشاشات

     إذا استمر الصداع أكثر من 3 أيام، يُنصح بزيارة الطبيب.

     هل تريد حجز جلسة استشارية؟"

👤: "نعم احجز لي"
🤖: "✅ تم حجز جلسة فيديو مع الدكتور محمد
     بتاريخ 2025-11-20 الساعة 14:00"
```

### مثال 2: تحليل صورة
```
👤: 📷 [صورة جرح] + "ما رأيك؟"
🤖: "شكراً على الصورة. بناءً على التحليل:

     📋 الملاحظات:
     • جرح سطحي نظيف
     • لا توجد علامات التهاب
     • الشفاء طبيعي

     ✅ التوصيات:
     • غسل المنطقة بالماء والصابون
     • وضع مرهم مضاد حيوي
     • تغطية الجرح

     ⚠️ درجة الخطورة: منخفضة

     الجرح يتعافى بشكل طبيعي ولا يحتاج تدخل طبي."
```

### مثال 3: حالة عاجلة
```
👤: 📷 [صورة طفح جلدي شديد] + "ظهر فجأة!"
🤖: "⚠️ تحذير: هذا الطفح يبدو شديداً

     📋 التحليل:
     • طفح جلدي واسع الانتشار
     • احمرار شديد
     • قد يكون رد فعل تحسسي

     🚨 درجة الخطورة: عالية

     هذه الحالة تتطلب استشارة طبية عاجلة!

     هل تريد حجز جلسة فورية مع طبيب؟"

👤: "نعم فوراً"
🤖: "✅ تم حجز جلسة عاجلة مع الدكتور سارة
     اليوم الساعة 15:30

     يرجى أن تكون مستعداً للجلسة."
```

---

## 🧪 الاختبار

### خطوات الاختبار:

#### 1. الدردشة العادية
```bash
✅ إرسال رسالة نصية
✅ استلام رد من AI
✅ محادثة متعددة
```

#### 2. رفع الصور
```bash
✅ الضغط على زر الصورة 📷
✅ اختيار صورة
✅ معاينة الصورة
✅ إرسال للتحليل
✅ استلام النتيجة
```

#### 3. حجز الجلسات
```bash
✅ طلب حجز بالنص
✅ AI يحجز تلقائياً
✅ استلام التأكيد
✅ ظهور البادج
✅ الجلسة في التبويب
```

#### 4. السيناريوهات المدمجة
```bash
✅ صورة + طلب حجز
✅ تحليل عاجل → حجز فوري
✅ استشارة → توصية → حجز
```

---

## ⚙️ إعداد n8n

### Workflow تحليل الصور (خطوات الإنشاء):

1. **افتح n8n**
   ```
   https://moodi-app.app.n8n.cloud
   ```

2. **أنشئ Workflow جديد**
   - اسم: "MOODi Image Analysis"

3. **أضف Webhook Node**
   - Method: POST
   - Path: `/analyze-image`
   - Response Mode: "Last Node"

4. **أضف Function Node: "Extract Data"**
   ```javascript
   const image = $json.image;
   const userId = $json.userId;
   const context = $json.context || '';

   return {
     json: {
       image,
       userId,
       context,
       prompt: `أنت طبيب استشاري متخصص. قم بتحليل هذه الصورة الطبية وتقديم:

       1. الملاحظات الرئيسية
       2. التشخيص المحتمل
       3. التوصيات العلاجية
       4. درجة الخطورة (low/medium/high)

       سياق إضافي من المريض: ${context}

       يرجى الرد بالعربية بشكل مفصل وواضح.`
     }
   };
   ```

5. **أضف HTTP Request Node: "OpenAI Vision"**
   ```
   Method: POST
   URL: https://api.openai.com/v1/chat/completions
   Headers:
     Authorization: Bearer YOUR_OPENAI_API_KEY
     Content-Type: application/json
   Body:
   {
     "model": "gpt-4-vision-preview",
     "messages": [{
       "role": "user",
       "content": [
         {"type": "text", "text": "{{$json.prompt}}"},
         {"type": "image_url", "image_url": {"url": "{{$json.image}}"}}
       ]
     }],
     "max_tokens": 1000
   }
   ```

6. **أضف Function Node: "Format Response"**
   ```javascript
   const aiResponse = $json.choices[0].message.content;

   // استخراج درجة الخطورة من النص
   let urgency = 'low';
   if (aiResponse.includes('عالية') || aiResponse.includes('عاجل')) {
     urgency = 'high';
   } else if (aiResponse.includes('متوسطة')) {
     urgency = 'medium';
   }

   return {
     json: {
       analysis: aiResponse,
       findings: aiResponse.split('\n\n')[0],
       recommendations: aiResponse,
       urgency: urgency
     }
   };
   ```

7. **فعّل الـ Workflow** ✅

---

## 📱 التكامل في التطبيق

### استخدام الوكيل:

```typescript
import { sendMessageToAI, analyzeImage, bookSessionViaAI } from '@/lib/ai-agent';

// دردشة عادية
const response = await sendMessageToAI("كيف أعالج الصداع؟", userId);

// تحليل صورة
const analysis = await analyzeImage(base64Image, userId, "طفح جلدي");

// حجز جلسة
const booking = await bookSessionViaAI(userId, {
  sessionType: 'video',
  notes: 'استشارة عاجلة'
});
```

---

## 🎯 الميزات المستقبلية

- [ ] دعم الصوت (تسجيل صوتي + تحليل)
- [ ] تحليل فيديوهات قصيرة
- [ ] AI يتذكر المحادثات السابقة
- [ ] توصيات دوائية مخصصة
- [ ] تكامل مع سجل المريض الطبي
- [ ] إشعارات ذكية بناءً على الحالة
- [ ] تحليل مخبري من الصور
- [ ] توقع الأمراض المحتملة

---

## ✅ التهاني!

تم تطوير وكيل ذكاء اصطناعي متقدم يمكنه:
- 💬 الدردشة الذكية
- 📷 تحليل الصور الطبية
- 📅 حجز الجلسات تلقائياً
- 🔗 التكامل السلس مع التطبيق

**جاهز للاستخدام!** 🚀✨
