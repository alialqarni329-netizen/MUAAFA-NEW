# إصلاح الطبيب الذكي - Smart Doctor Fix

## المشكلة
الطبيب الذكي لا يرد على الرسائل لأن OpenAI API Key غير مضاف في إعدادات Supabase.

---

## الحل (خطوات سريعة)

### الخطوة 1: احصل على OpenAI API Key

1. اذهب إلى: https://platform.openai.com/api-keys
2. سجل دخول أو أنشئ حساب
3. اضغط على "Create new secret key"
4. انسخ المفتاح (يبدأ بـ `sk-...`)

> ⚠️ **مهم**: احفظ المفتاح في مكان آمن، لن تستطيع رؤيته مرة أخرى!

---

### الخطوة 2: أضف المفتاح في Supabase

1. افتح Supabase Dashboard
2. اذهب إلى: **Project Settings** > **Edge Functions**
3. في قسم **Environment Variables**، اضغط **Add Variable**
4. أضف المتغير التالي:
   - **Name**: `OPENAI_API_KEY`
   - **Value**: المفتاح الذي نسخته من OpenAI
5. اضغط **Save**

---

### الخطوة 3: اختبر البوت

1. افتح التطبيق
2. اذهب إلى الطبيب الذكي
3. أرسل رسالة تجريبية مثل "مرحبا"
4. يجب أن يرد البوت خلال ثوانٍ

---

## التحديثات المطبقة في الكود

### 1. تحسين استدعاء Edge Function
- استبدلت `fetch()` بـ `supabase.functions.invoke()`
- إدارة أخطاء أفضل
- رسائل خطأ واضحة

### 2. إضافة تتبع الأخطاء
```typescript
console.error('Error sending message:', error);
console.error('Error details:', JSON.stringify(error));
```

### 3. عرض رسالة الخطأ الحقيقية
- الآن إذا حدث خطأ، سترى السبب الحقيقي في رسالة التنبيه

---

## اختبار الوظائف

بعد إضافة المفتاح، جرب:

1. ✅ إرسال رسالة عادية
2. ✅ إرسال عدة رسائل متتالية
3. ✅ اختبار الردود السريعة
4. ✅ التأكد من حفظ المحادثات

---

## الملفات المعدلة

- `app/smart-doctor.tsx` - تحسين استدعاء Edge Function وإدارة الأخطاء

---

## ملاحظات

### تكلفة OpenAI
- النموذج المستخدم: `gpt-4o-mini` (أرخص نموذج)
- التكلفة: ~$0.0001 لكل رسالة
- يوجد نظام تخزين مؤقت لتقليل التكلفة

### حدود الاستخدام
- حد يومي: 100 رسالة لكل مستخدم
- حد لكل دقيقة: 10 رسائل
- يمكن تعديل هذه الحدود من لوحة Owner

---

## إذا لم تنجح

### تحقق من:
1. المفتاح صحيح ويبدأ بـ `sk-`
2. الحساب في OpenAI لديه رصيد
3. المفتاح تم إضافته في Supabase Edge Functions (وليس Project Settings > API)

### رسائل الأخطاء الشائعة:
- **"OpenAI API key not configured"** → المفتاح غير مضاف في Supabase
- **"Invalid API key"** → المفتاح خاطئ
- **"Insufficient quota"** → ليس لديك رصيد في حساب OpenAI

---

## تم ✅

- إصلاح استدعاء Edge Function
- إضافة تتبع أخطاء محسن
- توثيق خطوات الإعداد
