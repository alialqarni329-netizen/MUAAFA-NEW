# 👑 لوحة إدارة MOODi Health

## 📋 نظرة عامة

تم إنشاء لوحة تحكم شاملة لإدارة تطبيق MOODi Health، تتيح للمسؤولين الوصول الكامل لإدارة المستخدمين والجلسات والإحصائيات.

## 🏗️ الهيكل

### الملفات المنشأة

```
lib/
  └── admin.ts                    # دوال مساعدة للإدارة

app/admin/
  ├── index.tsx                   # لوحة التحكم الرئيسية
  ├── users.tsx                   # إدارة المستخدمين
  ├── sessions.tsx                # إدارة الجلسات
  └── settings.tsx                # إعدادات الإدارة
```

## 🔐 نظام الصلاحيات

### الأدوار (Roles)

يدعم النظام 3 أدوار إدارية:

| الدور | الاسم بالعربية | الصلاحيات |
|------|----------------|-----------|
| `owner` | مالك | صلاحيات كاملة على كل شيء |
| `admin` | مدير | إدارة المستخدمين والجلسات |
| `moderator` | مشرف | عرض البيانات فقط |

### التحقق من الصلاحيات

يتم التحقق من صلاحيات المسؤول في كل صفحة:

```typescript
const admin = await checkIsAdmin(user.id);

if (!admin) {
  router.replace('/');  // إعادة التوجيه للصفحة الرئيسية
  return;
}
```

## 📊 لوحة التحكم الرئيسية

### المميزات

✅ **إحصائيات في الوقت الفعلي**
- إجمالي المستخدمين
- الاشتراكات النشطة
- الجلسات المكتملة
- إجمالي التسجيلات

✅ **الإدارة السريعة**
- زر سريع لإدارة المستخدمين
- زر سريع لإدارة الجلسات
- زر سريع للإعدادات

✅ **النشاط الأخير**
- آخر 5 مستخدمين مسجلين
- آخر 5 جلسات محجوزة

✅ **تحديث تلقائي**
- Pull to refresh
- تحديث الإحصائيات لحظياً

### الوصول

```
app/(tabs)/settings.tsx  →  زر "لوحة الإدارة" (يظهر للمسؤولين فقط)
                         ↓
app/admin/index.tsx      →  لوحة التحكم الرئيسية
```

## 👥 إدارة المستخدمين

### المميزات

✅ **عرض جميع المستخدمين**
- معلومات كاملة عن كل مستخدم
- حالة الاشتراك (نشط، تجريبي، منتهي)
- تاريخ التسجيل

✅ **البحث المتقدم**
- البحث بالاسم
- البحث برقم الجوال
- البحث بالمدينة

✅ **التنسيق الواضح**
- بطاقات منظمة لكل مستخدم
- أيقونات توضيحية
- ألوان مميزة للحالات

### البيانات المعروضة

```typescript
{
  full_name: string        // الاسم الكامل
  phone: string           // رقم الجوال
  location: string        // المدينة
  gender: string          // الجنس
  created_at: string      // تاريخ التسجيل
  subscriptions: []       // الاشتراكات
}
```

### حالات الاشتراك

| الحالة | اللون | الوصف |
|-------|-------|-------|
| **نشط** | 🟢 أخضر | اشتراك مدفوع نشط |
| **تجريبي** | 🟡 برتقالي | فترة تجريبية 5 أيام |
| **منتهي** | ⚪ رمادي | انتهى الاشتراك |
| **بدون اشتراك** | 🔴 أحمر | لم يشترك بعد |

## 📅 إدارة الجلسات

### المميزات

✅ **عرض جميع الجلسات**
- معلومات الجلسة الكاملة
- اسم المستخدم
- التاريخ والوقت
- الحالة

✅ **حالات الجلسة**
- 🔵 مجدولة (Scheduled)
- 🟢 مكتملة (Completed)
- 🔴 ملغاة (Cancelled)
- 🟡 جارية (In Progress)

✅ **التفاصيل**
- الملاحظات (إن وجدت)
- تاريخ الإنشاء
- معرف الجلسة (ID)

### البيانات المعروضة

```typescript
{
  id: string              // معرف الجلسة
  user_id: string         // معرف المستخدم
  scheduled_date: string  // التاريخ
  scheduled_time: string  // الوقت
  status: string          // الحالة
  notes?: string          // الملاحظات
  users: {
    full_name: string     // اسم المستخدم
  }
}
```

## ⚙️ إعدادات الإدارة

### المميزات

✅ **معلومات الحساب**
- الصلاحية (owner/admin/moderator)
- معرف المستخدم
- تاريخ الإنشاء

✅ **إعدادات النظام**
- قاعدة البيانات
- الإشعارات
- السجلات

✅ **تسجيل الخروج**
- تأكيد قبل الخروج
- حماية من الخروج العرضي

## 🎨 التصميم والواجهة

### الألوان

```typescript
// الألوان الأساسية
Primary:   #0ea5e9  // أزرق فاتح
Success:   #22c55e  // أخضر
Warning:   #f59e0b  // برتقالي
Error:     #ef4444  // أحمر
Purple:    #7c3aed  // بنفسجي (لوحة الإدارة)

// الألوان الثانوية
Blue:      #3b82f6
Pink:      #ec4899
Gray:      #64748b
```

### المكونات

#### بطاقة الإحصائيات
```
┌─────────────────────┐
│  [أيقونة]          │
│    250             │
│  إجمالي المستخدمين  │
└─────────────────────┘
```

#### بطاقة المستخدم
```
┌─────────────────────────────┐
│ [👤]  أحمد محمد    [نشط]   │
├─────────────────────────────┤
│ 📞 0501234567              │
│ 📍 الرياض                  │
│ 👤 ذكر                     │
│ 📅 2024/01/15              │
└─────────────────────────────┘
```

#### بطاقة الجلسة
```
┌─────────────────────────────┐
│ [مجدولة]        #abc12345  │
├─────────────────────────────┤
│ 👤 أحمد محمد               │
│ 📅 2024/02/15              │
│ ⏰ 10:00 صباحاً            │
│                            │
│ ملاحظات:                  │
│ استشارة أولية              │
└─────────────────────────────┘
```

## 🔄 تدفق العمل

### الدخول للوحة الإدارة

```
1. المستخدم يفتح التطبيق
   ↓
2. يذهب لصفحة الإعدادات
   ↓
3. إذا كان مسؤول → يظهر زر "لوحة الإدارة"
   ↓
4. يضغط على الزر
   ↓
5. التحقق من الصلاحيات
   ↓
6. عرض لوحة التحكم
```

### إدارة المستخدمين

```
1. فتح لوحة الإدارة
   ↓
2. الضغط على "إدارة المستخدمين"
   ↓
3. تحميل قائمة المستخدمين
   ↓
4. البحث والتصفية (اختياري)
   ↓
5. عرض معلومات كل مستخدم
```

## 🛡️ الأمان

### الحماية على مستوى قاعدة البيانات

```sql
-- جدول المسؤولين
CREATE TABLE admin_users (
  id uuid PRIMARY KEY REFERENCES auth.users(id),
  role text NOT NULL CHECK (role IN ('owner', 'admin', 'moderator')),
  created_at timestamptz DEFAULT now()
);

-- Row Level Security
ALTER TABLE admin_users ENABLE ROW LEVEL SECURITY;

-- فقط المسؤولون يمكنهم رؤية بعضهم
CREATE POLICY "Admins can view admin users"
  ON admin_users FOR SELECT
  TO authenticated
  USING (
    EXISTS (
      SELECT 1 FROM admin_users
      WHERE admin_users.id = auth.uid()
    )
  );
```

### الحماية على مستوى التطبيق

```typescript
// 1. التحقق من تسجيل الدخول
const { data: { user } } = await supabase.auth.getUser();
if (!user) {
  router.replace('/auth/login');
  return;
}

// 2. التحقق من الصلاحيات
const admin = await checkIsAdmin(user.id);
if (!admin) {
  router.replace('/');
  return;
}

// 3. الوصول للبيانات
// فقط بعد التحقق من الصلاحيات
```

## 📱 إضافة مسؤول جديد

لإضافة مسؤول جديد، استخدم Supabase SQL Editor:

```sql
-- إضافة مسؤول جديد
INSERT INTO admin_users (id, role)
VALUES (
  'user-uuid-here',  -- معرف المستخدم من جدول auth.users
  'admin'            -- owner, admin, or moderator
);
```

### الخطوات:

1. افتح Supabase Dashboard
2. اذهب إلى SQL Editor
3. انسخ والصق الكود أعلاه
4. استبدل `user-uuid-here` بمعرف المستخدم
5. اختر الدور المناسب
6. Run

## 🧪 الاختبار

### خطوات الاختبار:

1. **إضافة مسؤول**
   ```sql
   INSERT INTO admin_users (id, role)
   VALUES ('your-user-id', 'admin');
   ```

2. **تسجيل الدخول**
   - افتح التطبيق
   - سجل الدخول بحساب المسؤول

3. **الوصول للوحة**
   - اذهب للإعدادات
   - يجب أن يظهر زر "لوحة الإدارة"
   - اضغط عليه

4. **اختبار المميزات**
   - تحقق من الإحصائيات
   - افتح إدارة المستخدمين
   - افتح إدارة الجلسات
   - جرب البحث

### النتيجة المتوقعة:

✅ الزر يظهر فقط للمسؤولين
✅ لوحة التحكم تعمل بشكل صحيح
✅ جميع الإحصائيات تظهر
✅ البحث يعمل بدقة
✅ التصميم جميل ومنظم

## 📊 الإحصائيات المتوفرة

```typescript
{
  totalUsers: number          // إجمالي المستخدمين
  activeSubscriptions: number // الاشتراكات النشطة
  completedSessions: number   // الجلسات المكتملة
  totalRecordings: number     // إجمالي التسجيلات
}
```

## 🎯 الميزات المستقبلية المقترحة

- [ ] إحصائيات متقدمة بالرسوم البيانية
- [ ] إمكانية تعديل بيانات المستخدمين
- [ ] إمكانية إلغاء/إعادة جدولة الجلسات
- [ ] نظام إشعارات للمسؤولين
- [ ] تصدير البيانات (Excel/PDF)
- [ ] سجل النشاطات (Activity Log)
- [ ] لوحة تحكم بالإحصائيات المالية
- [ ] إدارة المدفوعات
- [ ] نظام التقارير الشهرية
- [ ] إدارة الأطباء والمعالجين

## 🔧 الصيانة

### مراقبة الأداء
- تحقق من سرعة تحميل البيانات
- راقب عدد الاستعلامات
- تحقق من حجم البيانات المعروضة

### التحديثات الدورية
- تحديث الإحصائيات بانتظام
- مراجعة صلاحيات المسؤولين
- تنظيف البيانات القديمة

---

تم إنشاء لوحة التحكم بنجاح! 🎉

## 🚀 البدء السريع

1. أضف نفسك كمسؤول في قاعدة البيانات
2. افتح التطبيق وسجل الدخول
3. اذهب للإعدادات
4. اضغط على "لوحة الإدارة"
5. استمتع بالإدارة! ✨
