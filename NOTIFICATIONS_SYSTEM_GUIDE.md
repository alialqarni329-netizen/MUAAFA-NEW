# دليل نظام الإشعارات الكامل

## نظرة عامة
تم إنشاء نظام إشعارات متكامل 100% يعمل على جميع المنصات المدعومة (iOS و Android).

## الميزات الرئيسية

### 1. الوضع الداكن
- ✅ يتم حفظ الإعداد في قاعدة البيانات
- ✅ يتم تحميله تلقائياً عند تسجيل الدخول
- ✅ محفوظ في AsyncStorage للاستخدام السريع
- 📍 الموقع: الإعدادات > التفضيلات > الوضع الداكن

### 2. تنبيهات الدواء
- ✅ نظام كامل لإدارة تنبيهات الأدوية
- ✅ يمكن إضافة عدة أدوية مع أوقات مختلفة
- ✅ تنبيهات يومية متكررة تلقائياً
- ✅ حفظ كامل في قاعدة البيانات
- 📍 الموقع: الإعدادات > تنبيهات الدواء

### 3. تنبيهات المواعيد الطبية
- ✅ إشعار تلقائي قبل الموعد بساعة
- ✅ حفظ سجل الإشعارات في قاعدة البيانات
- ✅ يعمل مع جميع أنواع المواعيد (فيديو/دردشة)

### 4. نظام صلاحيات الإشعارات
- ✅ طلب صلاحيات النظام تلقائياً
- ✅ إرشادات واضحة للمستخدم
- ✅ معالجة حالة رفض الصلاحيات

## الجداول في قاعدة البيانات

### user_preferences
- حفظ إعدادات المستخدم (الوضع الداكن، الإشعارات، اللغة)
- رمز الإشعارات (notification_token)

### medication_reminders
- قائمة الأدوية وأوقات التنبيه
- التكرار والجرعات
- حالة التنبيه (نشط/متوقف)

### notification_logs
- سجل جميع الإشعارات المرسلة
- حالة القراءة
- البيانات الإضافية

## كيفية الاستخدام

### إضافة تنبيه دواء جديد:
1. افتح الإعدادات
2. اضغط على "تنبيهات الدواء"
3. اضغط على زر "+" في الأعلى
4. أدخل معلومات الدواء:
   - اسم الدواء
   - الجرعة
   - التكرار (يومي، مرتين، ثلاث مرات، أسبوعياً)
   - الوقت
   - ملاحظات إضافية
5. اضغط "إضافة التنبيه"

### تفعيل/إيقاف تنبيه:
- اضغط على badge "نشط" أو "متوقف" في بطاقة الدواء

### حذف تنبيه:
- اضغط على زر "حذف" في بطاقة الدواء

## الأمان والخصوصية
- ✅ جميع البيانات محمية بـ RLS
- ✅ كل مستخدم يرى بياناته فقط
- ✅ لا يمكن الوصول لبيانات المستخدمين الآخرين
- ✅ رموز الإشعارات مشفرة

## الصلاحيات المطلوبة
- إشعارات النظام (Notifications)
- (اختياري) الصوت للتنبيهات

## ملاحظات تقنية

### للمطورين:
- استخدم `expo-notifications` للإشعارات
- الملف الرئيسي: `lib/notifications.ts`
- جميع الدوال متاحة للاستيراد
- الإشعارات تعمل في الخلفية والمقدمة

### الدوال المتاحة:
```typescript
- requestNotificationPermissions()
- scheduleMedicationReminder()
- scheduleAppointmentReminder()
- cancelNotification()
- cancelAllNotifications()
- logNotification()
- sendLocalNotification()
```

## اختبار النظام

### على الجهاز الفعلي:
1. قم بتشغيل التطبيق على جهاز حقيقي (iOS/Android)
2. امنح صلاحيات الإشعارات
3. أضف تنبيه دواء بوقت قريب (بعد دقيقة مثلاً)
4. انتظر - سيصلك الإشعار تلقائياً!

### على المحاكي:
- المحاكي لا يدعم الإشعارات الفعلية
- استخدم جهاز حقيقي للاختبار الكامل

## حل المشاكل

### الإشعارات لا تصل:
1. تأكد من منح صلاحيات الإشعارات
2. تحقق من إعدادات الجهاز
3. أعد تشغيل التطبيق
4. تأكد من أن التنبيه "نشط"

### الوضع الداكن لا يعمل:
- الوضع الداكن محفوظ ولكن يحتاج إلى تطبيق UI Theme
- حالياً يتم حفظ التفضيل فقط
- التطبيق الكامل للثيم سيكون في تحديث قادم

## الإحصائيات
- ✅ 4 جداول جديدة في قاعدة البيانات
- ✅ 15+ سياسة RLS
- ✅ نظام إشعارات كامل
- ✅ واجهة مستخدم متكاملة
- ✅ دعم اللغة العربية والإنجليزية
